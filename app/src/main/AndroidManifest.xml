<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nas">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".notificacion"></activity>
        <activity android:name=".mostrar_ninera" />
        <activity android:name=".listar_perfiles" />
        <activity android:name=".listar_perfiles" />
        <activity android:name=".buscarUsuario" />
        <activity
            android:name=".administrador"
            android:label="@string/title_activity_administrador"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".mapbox" />
        <activity android:name=".Reservarninera" />
        <activity android:name=".home1" />
        <activity android:name=".nav_home2" />
        <activity android:name=".Cambiarninera" />
        <activity android:name=".Cambiarpadre" />
        <activity android:name=".Actualizarninera" />
        <activity android:name=".Actualizarpadre" />
        <activity android:name=".calificacion" />
        <activity android:name=".solicitarninera" />
        <activity android:name=".listar_ninera" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".registroN" />
        <activity android:name=".mostrar" />
        <activity
            android:name=".ninera"
            android:label="@string/title_activity_ninera"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".padre"
            android:label="@string/title_activity_padre"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".registro" />
        <activity android:name=".registropadre" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>